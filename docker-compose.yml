version: '3.8'

services:
  backend:
    build: ./backend/usdt-event 
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production

  frontend:
    build: ./frontend/usdt-event-app 
    ports:
      - "5173:5173"
    depends_on:
      - backend